<p>
    <button id="{{id}}" class="btn btn-menu btn-secondary btn-block" lesson="{{lesson}}">
        {{text}} {{#if complete}}<i class="fa fa-check-circle position-right text-success complete-check"></i>{{/if}}
    </button>
</p>
